<h1>Edit Message</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.contact_message_path(@conn, :update, @contact_id, @message)) %>

<span><%= link "Back", to: Routes.contact_message_path(@conn, :index, @contact_id) %></span>
