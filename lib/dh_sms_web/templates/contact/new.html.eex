<h1>New Contact</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.conversation_contact_path(@conn, :create, @conversation_id)) %>

<span><%= link "Back", to: Routes.conversation_contact_path(@conn, :index, @conversation_id) %></span>
